version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: minh-trang-academy
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
